# Animations

Phase 2 goal: replicate upstream Lab-Blueprint/Solana-Rapid-Fire-Beginner-101 animations as closely as possible, but embedded as React components.

- Use GSAP timelines in `useEffect` with cleanup.
- Keep DOM/SVG structure close to upstream IDs so timing + selectors map 1:1.
